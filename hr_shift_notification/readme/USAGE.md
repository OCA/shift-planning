1. Open a shift planning. The *To Notify* button counts the shifts that have changed since the last notification.
2. Click it to open the wizard, uncheck any employee to skip, and click *Send*.

**Opt-out.** From *Preferences*, a user can turn shift notifications off for themselves. For an employee without a user account, an HR manager sets the flag on the employee form.
